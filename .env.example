VIRUSTOTAL_API_KEY=your_api_key_here
SECRET_KEY=change-this-to-a-long-random-string

# Gmail SMTP (local only — Render free blocks SMTP)
MAIL_USERNAME=your.email@gmail.com
MAIL_PASSWORD=your_gmail_app_password
MAIL_DEFAULT_SENDER=your.email@gmail.com

# Brevo HTTPS (preferred on Render) — free tier, send OTP to any user email.
# 1) Create account at https://app.brevo.com
# 2) Senders → Add a sender → verify your Gmail
# 3) SMTP & API → API keys → create key
BREVO_API_KEY=
BREVO_FROM=your.email@gmail.com
BREVO_FROM_NAME=CyberScan

# Optional Resend fallback (sandbox only delivers to your Resend account email)
# RESEND_API_KEY=
# RESEND_FROM=CyberScan <onboarding@resend.dev>

# Local device testing: http://YOUR_LAN_IP:5000
# Render production: https://your-service.onrender.com  (no trailing slash)
APP_BASE_URL=http://127.0.0.1:5000

# Optional: set false only on local PCs with broken SSL (Avast). Use true on Render.
VIRUSTOTAL_SSL_VERIFY=true

# Render: one-time first-admin bootstrap (delete after creating admin)
# ADMIN_SETUP_SECRET=long-random-string
