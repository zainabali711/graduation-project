# Secrets — never push to GitHub / Render from git
.env
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.DS_Store
*.db
instance/

# Large local datasets (keep scripts/summaries; not the multi‑GB CSV)
data/dns_dataset.csv
